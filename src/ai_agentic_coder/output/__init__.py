# This file marks the 'output' directory as a Python package so
# that relative imports (e.g., from .accounts import Account) work
# correctly when launching the Gradio app as a module.

__all__ = []
